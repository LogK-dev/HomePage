---
import Footer from "@/components/Footer.astro";
import Header from "@/components/Header.astro";
import AnswerCompareMockup from "@/components/mockups/AnswerCompareMockup.astro";
import CreditWalletMockup from "@/components/mockups/CreditWalletMockup.astro";
import ModelSelectionMockup from "@/components/mockups/ModelSelectionMockup.astro";
import PolicyControlMockup from "@/components/mockups/PolicyControlMockup.astro";
import type { Locale, SiteDictionary } from "@/i18n/schema";
import { getLanguageOptions, localizedPath } from "@/utils/i18n";

interface Props {
  locale: Locale;
  dictionary: SiteDictionary;
}

const { locale, dictionary } = Astro.props;
const homePath = localizedPath(locale);
const productPath = localizedPath(locale, "product");
const languageOptions = getLanguageOptions(locale, "mockups");
const copy = dictionary.mockupsPage;
---

<Header locale={locale} dictionary={dictionary} languageOptions={languageOptions} />

<main>
  <section class="mockups-hero section">
    <div class="section-head" data-reveal>
      <p class="eyebrow">{copy.eyebrow}</p>
      <h1>{copy.title}</h1>
      <p>{copy.lede}</p>
      <p>{copy.note}</p>
      <div class="hero-actions">
        <a class="button" href={productPath}>{copy.primaryCta}</a>
        <a class="button button-ghost" href={homePath}>{copy.secondaryCta}</a>
      </div>
    </div>
  </section>

  <section class="section mockups-grid">
    <article class="mockup-scene" data-reveal>
      <div class="mockup-scene-head">
        <p class="eyebrow">{copy.scenes[0].eyebrow}</p>
        <h2>{copy.scenes[0].title}</h2>
        <p>{copy.scenes[0].body}</p>
      </div>
      <ModelSelectionMockup locale={locale} />
    </article>

    <article class="mockup-scene" data-reveal>
      <div class="mockup-scene-head">
        <p class="eyebrow">{copy.scenes[1].eyebrow}</p>
        <h2>{copy.scenes[1].title}</h2>
        <p>{copy.scenes[1].body}</p>
      </div>
      <AnswerCompareMockup locale={locale} />
    </article>

    <article class="mockup-scene" data-reveal>
      <div class="mockup-scene-head">
        <p class="eyebrow">{copy.scenes[2].eyebrow}</p>
        <h2>{copy.scenes[2].title}</h2>
        <p>{copy.scenes[2].body}</p>
      </div>
      <PolicyControlMockup locale={locale} />
    </article>

    <article class="mockup-scene" data-reveal>
      <div class="mockup-scene-head">
        <p class="eyebrow">{copy.scenes[3].eyebrow}</p>
        <h2>{copy.scenes[3].title}</h2>
        <p>{copy.scenes[3].body}</p>
      </div>
      <CreditWalletMockup locale={locale} />
    </article>
  </section>
</main>

<Footer />
